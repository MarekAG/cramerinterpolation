<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.numericmethods.marekag.cramerinterpolation" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTop"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Polynomial"
            android:label="@string/title_activity_polynomial"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.numericmethods.marekag.cramerinterpolation.MainActivity" />
        </activity>
        <activity
            android:name=".Derivative"
            android:label="@string/title_activity_derivative"
            android:parentActivityName=".Polynomial" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.numericmethods.marekag.cramerinterpolation.Polynomial" />
        </activity>
    </application>

</manifest>
